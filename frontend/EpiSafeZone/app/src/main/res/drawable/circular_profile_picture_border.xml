<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer Circle (Thin Border) -->
    <item>
        <shape android:shape="oval">
        <stroke
            android:width="3dp"
            android:color="@color/epiGreen"/>
        </shape>
    </item>
</layer-list>